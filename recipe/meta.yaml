{% set name = "fanstatic" %}
{% set version = "1.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 881f0a26f18f79ff658d037b9462273abd86bd8680ba7d77f7aa7361787f9f43

build:
  noarch: python
  number: 0
  entry_points:
    - fanstatic-compile = fanstatic.compiler:compile_resources
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools
  run:
    - python >=3.6
    - setuptools
    - shutilwhich
    - webob >=1.2

test:
  imports:
    - fanstatic
  commands:
    - fanstatic-compile --help
  requires:
    - closure
    - cssmin
    - jsmin
    - pytest >=2.3

about:
  home: http://fanstatic.org
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: Flexible static resources for web applications
  doc_url: http://www.fanstatic.org/en/latest/
  dev_url: https://bitbucket.org/fanstatic/fanstatic/src/default/

extra:
  recipe-maintainers:
    - mxr-conda
    - mwilson8
